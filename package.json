{
    "name": "noise_cancelling",
    "version": "v0.0.1",
    "scripts": {
        "version:auto-patch": "npm version patch --no-git-tag-version --allow-same-version --message \"chore(version): auto patch version bump [skip ci]\" && git add package.json package-lock.json"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}